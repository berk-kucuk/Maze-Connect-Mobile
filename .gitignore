build/
.gradle/
local.properties
.kotlin/
*.iml
.idea/
.vscode/
captures/
.cxx/
*.hprof
*.jks.bak

# Signing material — never commit
*.keystore
*.jks

# Anchored to the root. Unanchored, `keystore/` also matched the source
# package core/.../keystore/, so AndroidKeyStoreManager.kt was never
# committed and a clean clone did not compile — F-Droid's build found it.
/keystore/
/keystore.properties
dist/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
*.bak
*~
*.swp
*.swo
